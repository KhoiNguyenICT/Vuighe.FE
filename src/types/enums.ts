export const AppDataStorageKey = 'Vuighe_Storage'

export enum SortDirection {
    ascending = 0,
    descending = 1,
}

export enum FileType {
    library,
    image,
    video,
    audio,
    undefined
}
